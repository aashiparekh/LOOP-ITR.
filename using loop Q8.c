#include<stdio.h>
#include<conio.h>
void main() {
int i, n, sum;

printf("enter the value of n: ");
scanf("%d", &n);

for(int i; i<=n; i++);
sum=sum + (2*i - 1);

printf("enter the sum of first n odd numbers=%d\n");
 return 0;
 }
